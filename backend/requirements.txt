# ============================================================
# AI Roadmap Platform — backend dependencies
# Pin exact versions; bump deliberately.
# Run `pip-audit` monthly and update in small PRs.
# ============================================================

# ----- Web framework -----
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12      # form data for file uploads if ever needed

# ----- Config -----
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# ----- Database -----
sqlalchemy==2.0.36
aiosqlite==0.20.0
alembic==1.13.3

# ----- Auth -----
authlib==1.3.2                # Google OAuth2
itsdangerous==2.2.0           # signed cookies for OAuth state
python-jose[cryptography]==3.3.0   # JWT issue/verify

# ----- HTTP client (outbound) -----
httpx==0.27.2

# ----- OpenAI (embeddings only — semantic topic dedup) -----
openai==1.54.3

# ----- Email -----
aiosmtplib==3.0.2
email-validator==2.2.0

# ----- Rate limiting -----
slowapi==0.1.9

# ----- Templating (admin UI + share pages) -----
jinja2==3.1.4

# ----- Certificates (PDF rendering + QR) -----
# weasyprint pulls in cairo/pango at the system level — Dockerfile installs
# libpango-1.0-0 libharfbuzz0b libpangoft2-1.0-0 to satisfy the runtime deps.
weasyprint==63.1
pydyf==0.11.0
qrcode[pil]==7.4.2

# ----- Image rendering (OG social cards — SEO-11) -----
# Explicit pin; qrcode[pil] already pulls Pillow in transitively, but declaring
# it here makes the dependency load-bearing and survives qrcode removing [pil].
Pillow==11.0.0

# ----- Utilities -----
python-dateutil==2.9.0
orjson==3.10.7                # faster JSON for responses

# ----- Dev / test -----
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-httpx==0.30.0
pytest-cov==5.0.0

# ----- Security scanning -----
pip-audit==2.7.3
